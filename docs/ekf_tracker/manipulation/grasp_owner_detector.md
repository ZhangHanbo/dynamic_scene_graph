# `ekf_tracker/manipulation/grasp_owner_detector.py`

`GraspOwnerDetector` — three-tier (perception override → URDF inside-jaws containment → fallback) decision; emits `HeldDecision`.

```{eval-rst}
.. automodule:: ekf_tracker.manipulation.grasp_owner_detector
   :members:
   :show-inheritance:
```
