# `ekf_tracker/relations/relation_client.py`

Backend interface for REST + LLM relation detection; both return an `(N, N)` `p_parent` matrix in `[0, 1]`.

```{eval-rst}
.. automodule:: ekf_tracker.relations.relation_client
   :members:
   :show-inheritance:
```
