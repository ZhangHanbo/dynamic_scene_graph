# `ekf_tracker/configs/__init__.py`

`load_config()` (with `_extends:` deep-merge), plus `to_bernoulli_config`, `to_trigger_config`, and ~15 builder functions for noise schedules and subsystem kwargs.

```{eval-rst}
.. automodule:: ekf_tracker.configs
   :members:
   :show-inheritance:
```
