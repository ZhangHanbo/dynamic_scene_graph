# `ekf_tracker/config.py`

`BernoulliConfig` (fast-tier params) and `TriggerConfig` (slow-tier scheduling) dataclasses. Construct via `configs.to_*_config()`.

```{eval-rst}
.. automodule:: ekf_tracker.config
   :members:
   :show-inheritance:
```
