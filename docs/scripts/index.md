# `scripts/`

Visualization scripts that turn cached trajectories into images, videos,
or point clouds. They read from `tests/visualization_pipeline/<traj>/`
and `datasets/<traj>/` and produce per-frame artifacts you can eyeball
or assemble into a video.

```{toctree}
:maxdepth: 1

visualize_ekf_tracking
visualize_voxel_obs
render_gripper_overlay
visualize_arm_mask
```
